# Portfolio UI kit

A single-page filmmaker portfolio: nav, hero, work grid (click a card for a full-bleed detail view), contact form, footer. Composed entirely from `components/core`, `components/forms`, `components/navigation` — no bespoke primitives.

Copy (name, project titles, cities) is placeholder — no real product copy was provided in the source material. Replace before shipping.
